export default function ActivacionesHome() {
  return (
    <div className="rounded-lg border border-dashed border-gray-300 bg-white p-8 text-center">
      <h1 className="text-xl font-semibold text-gray-900">CiMA Activaciones</h1>
      <p className="mx-auto mt-2 max-w-md text-sm text-gray-500">
        Módulo en construcción. La lógica de dominio vive en{' '}
        <code className="rounded bg-gray-100 px-1 py-0.5 text-xs">modules/activaciones/</code> y las
        rutas bajo <code className="rounded bg-gray-100 px-1 py-0.5 text-xs">app/(activaciones)/</code>.
      </p>
    </div>
  );
}
